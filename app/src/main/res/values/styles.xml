<resources>
    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorScreenBackgound</item>
        <item name="android:itemBackground">@color/colorScreenBackgound</item>
        <item name="listPopupWindowStyle">@style/PopupList</item>
        <item name="colorControlHighlight">@color/colorRipple</item>
        <item name="android:popupMenuStyle">@style/PopupList</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="ExpandedAppBar" parent="@android:style/TextAppearance.Material.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/movie_details_expanded_toolbar_text_size</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CollapsedAppBar" parent="@android:style/TextAppearance.Material.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="PopupList" parent="@style/android:Widget.ListPopupWindow">
        <item name="android:textColor">@color/colorDetailsText</item>
        <item name="android:popupBackground">@color/colorScreenBackgound</item>
    </style>
</resources>
